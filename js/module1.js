const conHello = "안녕하세요";
console.log(conHello);
const fnPlusNumbers = (pNum1, pNum2) => pNum1 + pNum2;

export default fnPlusNumbers;
// export default 객체명;
// 대표로 하나만 export 할때
// import 문에서 새로운 객체를 만들 수 있음(이름을 변경할 수 있음)
